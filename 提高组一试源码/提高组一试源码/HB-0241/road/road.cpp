#include<stdio.h>
#include<iostream> 
int main()
{
	FILE *fin,*fout;
	int i,j,n,l,r,a,d;
	fin=fopen("road.in","r");
	fout=fopen("road.out","w");
	fscanf(fin,"%d %d",&n);
	for(i=1;i<=n;i++)
	{
		 while(d>0,(l>=1)&&(r<=6))
		 {
		 	l,r=d-1;
		 	for(i=1;i<=n;i++)
		 }
		 while(d>0,(l>=1)&&(r<=6))
		 {
		    d==d-1;
		 	for(i=1;i<=n;i++)
		 }
		 while(d>0,(l>=1)&&(r<=2))
		 {
		 	d==d-1; 
		 	for(i=1;i<=n;i++)
		 }
		 while(d>0,(l>=1)&&(r<=1))
		 {
		 	d==d-1;
		 	for(i=1;i<=n;i++)
		 }
		 while(d>0,(l>=4)&&(r<=6))
		 {
		 	d==d-1;
		 	for(i=1;i<=n;i++)
		 }
		 while(d>0,(l>=4)&&(r<=6))
		 {
		    d==d-1;
		 	for(i=1;i<=n;i++)
		 }
		for(j=1;j<=n;j++)
    } 
	fprintf(fout,"%d %d",&n);
	fclose(fin);
	fclose(fout);
    return 0;
}
