#include<stdio.h>
#include<string.h>
#include<iostream>
using namespace std;
int daolu[50000][50000][50000];
int main()
{
	int n, m;
	freopen("track.in", "r", stdin);
	freopen("track.out", "w", stdout);
	cin>>n>>m;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
