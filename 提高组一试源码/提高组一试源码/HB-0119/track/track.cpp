#include<iostream>
#include<cstring>
#include<algorithm>
#include<cstdio>
#include<functional>
#include<time.h>
#include<stdio.h>
using namespace std;
int main()
	{
		freopen("track.in","r",stdin);
		freopen("track.out","rw",stdout);
	
		int a,b[10000],c[10000];
		cin>>a;
		for(int i=0;i<a;i++)
		{
			for(int y=0;y<3;y++)
	        {
	        	cin>>b[y];
			}
			for(int u=0;u<3;u++)
	        {
	        	cin>>c[u];
			}
		printf("rand%()x");
		}
		return 0;
		}
