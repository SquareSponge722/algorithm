#include "iostream"
#include "cstdio"
#include "cstring"
#include "time.h"

void main(){
	freopen("road.in","r",stdin)
	freopen("road.out","rw",stdout)
	
	if (n) and (a[i]=di) 
	then writeln ("L")
	elsebegin 
	for 1:=to n-1 do
	begin
	if kand(a[n]<a[n+1])
	then continue else i:=false;
	if(not k) and (not i)break;
	end;
	if korl
	then writeln("0")
	else writeln (ndiv 2);
	end;
	
	
}
