#include<iostream>
#include<cstdio>
 using namespace std;
 int main(){
 	freopen("in.txt","r",std)
 	freopen("out.txt","w",std)
 	char T;
 	char n;
 	string a;
 	int x;int y;
 	cin>>T>>n>>a;
 	char i;0<i<n;
 	char l;a[l]>=a[i];
 	char s;a[s]<=a[i];
 	if(a[i]=2,a[i]=3){
 		cout<<2;
	 }
	 if(a[i]=1){
	 	cout<<1;
	 }
	 if(a[l]<2*a[s]){
	 	cout<<n;
	 }
	else while(a[i]=x*a[i],a[i]=x*a[i]+y*a[i]){
	    n-1;
		cout<<n;
	}
	 
 }
