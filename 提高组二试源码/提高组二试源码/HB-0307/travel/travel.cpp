#include<iostream>
#include<cstdio>
#include<cstdlib>
#include<cstring>
using namespace std;
int main()
{
	fropen("travel.in","r",stdin)
	fropen("travel.out","w",stdout)
	int n,m,a[10000],f,k;
	cin>>n>>m>>endl;
	for(int i;i<=n;i++)
	{
		cin>>a[i]>>b[i]>>endl;
	}
	a[i+1]=b[i+1];
	for(int k;k<=n;k++)
	b[i+1]=a[i+2];
	if
	a[i]=!0;
	a[i]=1;
	for(int f;f<=n;f++)
	a[i+1]=n;
	struct{
		fucuntion 
		string a,b,c;
		a[i]+c=b[i];
		if
		a[i]=b[i];
		a[i]+b[i]=0;
		return 1;
	}a[1000000];
	if
	name sure [i]+1;
	name[i+1];
	sure [io+1]=a[2]+a[3]+a[4]+a[5];
	Ccin>>deft cury a[nameisp]
	;
	if
	cout+7=8
	so
	for(int sls<=mhjs^u*USYU##)
	cture a[100000]
	if
	orfudiaoa{0--2nkksal(jkslaiqo);
	int a[i+2]b[i+1];
	void sure(int r,j)
	 sure a[i+1];
	b(b+2)
	cout nacyz 9;
	a[i+1]=b[2]	
	a[k]=b[i];
	knowesurie-bing jius{
	over comeing a[i]=1}
	if
	obwe
	cony a[w]=win deft,cou<<ou<<n<<m;
	if a>b;
	swap(a,b)
	else
	d=!;
	a[i]+niace ;fouyr{ai(".re",kuci,yici a[i==7];)}
	a[i]=0;
	return 0;
}
