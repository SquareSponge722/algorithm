#include <bits/stdc++.h>
using namespace std;
int a[5005],b[5005]

//freopen("travel.in","r",stdin)
//freopen("travel.out","w",stdout)
int main() 
{
	int n,m,u,v;
	cin>>n>>m;
	for(int i=0;i<=n;i++)
	{
		if(m=n-1)
			a[i]=b[i];
			a[u]<b[u];
			a[v]<b[v];
			swap(a[u],b[v])
	}
	
	return 0;
}
