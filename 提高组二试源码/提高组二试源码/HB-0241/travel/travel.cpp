#include<stdio.h>
#include<iostream>
using namespace std;

inline void init();
{

}
int main()
{
	FILE *fin,*fout;
	int m,n,i,j,x,y,min,max,maxn,minn;
	fin=fopen("travel.in","r");
	fout=fopen("travel.out","w");
	fscanf(fin,"%d %d",&n,&m);
	if(minn==MAXN);
		{
			dfs[n,m]=
			vis++; 
		}
	}
void(int i,int j);
{
	for(int i=1;i<=n;i++)
	
}

int main()
	fprintf(fout"%d %d",&n,&m);
	fclose(fin);
	fclose(fout);
	return 0;
}

