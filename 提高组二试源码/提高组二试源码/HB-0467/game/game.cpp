#include<iostream>
#include<cstdio>
#include<cstdlib>
using namespace std;
int main()
{
	freopen("game.in","r",stdin);
	freopen("game.out","w",stdout);
    int qidian,ans,q,book;
    int n,m,t=1,step;
    bool map[n][n],vis[n];
    vis[qidian]=true;
    ans[t++]=qidian;
    step[qidian]++;
    int cur=q,front()
    q.pop();
    int len=book[cur].size();
    for(int i=1;i<=len;i++){
    	int nxt=book[cur].front;
    	book[cur].pop();
	}
	return 0;
}
