#include <iostream>
#include <cstring>
#include <cstdio>
#include <cmath>
#include <cstdlib>
#include <algorithm>
using namespace std;
int main(int argc, char** argv)
 {
 	freopen("travel.in","r",stdin);
 	freopen("travel.out","w",stdout);
    int	 u,v,n,m,a,b,k;
    {
	cin>>n>>m;
    for(int i=1;i<=n;i++);
    1<=u;
    v<=n;
    a=0;
      {if(1<=u<m)
       u=v;
      else
      					
	  if(u<v)
      for(int i=1;i<=n;i++);
      k=k+1;
      }
    cout<<k;
	return 0;
	}
	
}
