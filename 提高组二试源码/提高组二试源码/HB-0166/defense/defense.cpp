#include <vector>
#include <fstream>
using namespace std;
ifstream in("defense.in");
ofstream out("defense.out");
vector<int*> v;
int main()
{
	while (true)
		v.push_back(new int);
	return 0;
}
